import React from "react";
import { UIContainerWrapper } from "./UIContainer.styles";

const UIContainer = ({ children }) => {
  return <UIContainerWrapper>{children}</UIContainerWrapper>;
};

export default UIContainer;
